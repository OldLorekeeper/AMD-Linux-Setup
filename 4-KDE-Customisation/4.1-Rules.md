r## Desktop and Laptop

Transparency and Blur (just in case)

```
> Window class (application) > Exact Match > plasmashell
> Match whole window class > ☑ 
> Window types: Dock (Panel) | Desktop
> Active opacity > Force > 70%
> Inactive opacity > Force > 70%
```

Hide OpenGL renderer

```
> Window class (application) > Unimportant
> Match whole window class > ☐ Yes ☑ No
> Window types: Normal windows
> Window title > Exact Match > OpenGL Renderer
> Skip taskbar > Apply initially > ☑ Yes ☐ No
```

PiP Above

```
> Window class (application) > Unimportant
> Match whole window class > ☐ Yes ☑ No
> Window types: All selected
> Window title > Exact Match > Picture-in-picture
> Keep above other windows > Force > ☑ Yes ☐ No
```

<br/>
 
## Desktop - 3440x1440

Start Small

```
>>> Window class (application) > Regular expression >(konsole|systemsettings|dolphin) 
>>> Match whole window class > ☑ No 
>>> Window types: Normal window 
>>> Window title > Regular expression > (^(?!Create|Configure|Properties|Confirmation|Confirm|Save|Edit|Delete Permanently|File Already Exists|Apply Settings|Confirm Close|Error).*$) 
>>> Size > Apply initially > 1000 x 650 
>>> Initial placement > Force > Centred 
>>> Ignore requested geometry > Force > ☑ Yes
```

Start Boxy

```
>>> Window class (application) > Regular expression > (net.lutris.Lutris) 
>>> Match whole window class > ☑ No 
>>> Window types: Normal window 
>>> Window title > Regular expression > ^Lutris$ 
>>> Size > Apply initially > 1500 x 950 
>>> Initial placement > Force > Centred 
>>> Ignore requested geometry > Force > ☑ Yes
```

Start Tall

```
>>> Window class (application) > Regular expression > (kate) 
>>> Match whole window class > ☑ No 
>>> Window types: Normal window 
>>> Window title > Regular expression > ^(?!Close|Configure|Save File|Specify).*$ 
>>> Size > Apply initially > 1200 x 1200 
>>> Initial placement > Force > Centred 
>>> Ignore requested geometry > Force > ☑ Yes
```

Start Wide

```
>>> Window class (application) > Regular expression > (google-chrome|steam|chrome-[a-z0-9]+-Default|obsidian) 
>>> Match whole window class > ☑ No 
>>> Window types: Normal window 
>>> Window title > Regular expression > (.* - Google Chrome$)|(^Steam$)|(.*) 
>>> Size > Apply initially > 2000 x 1250 
>>> Initial placement > Force > Centred 
>>> Ignore requested geometry > Force > ☑ Yes
```

<br/>
 
## Laptop - 2560x1600

Start Small

```
>>> Window class (application) > Regular expression >(konsole|systemsettings|dolphin) 
>>> Match whole window class > ☑ No 
>>> Window types: Normal window 
>>> Window title > Regular expression > (^(?!Create|Configure|Properties|Confirmation|Confirm|Save|Edit|Delete Permanently|File Already Exists|Apply Settings|Confirm Close|Error).*$)
>>> Size > Apply initially > 900 x 600
>>> Initial placement > Force > Centred 
>>> Ignore requested geometry > Force > ☑ Yes
```

Start Boxy

```
>>> Window class (application) > Regular expression > (net.lutris.Lutris) 
>>> Match whole window class > ☑ No 
>>> Window types: Normal window 
>>> Window title > Regular expression > ^Lutris$ 
>>> Size > Apply initially > 1116 x 1056 
>>> Initial placement > Force > Centred 
>>> Ignore requested geometry > Force > ☑ Yes
```

Start Tall

```
>>> Window class (application) > Regular expression > (kate) 
>>> Match whole window class > ☑ No 
>>> Window types: Normal window 
>>> Window title > Regular expression > ^(?!Close|Configure|Save File|Specify).*$ 
>>> Size > Apply initially > 890 x 1100
>>> Initial placement > Force > Centred 
>>> Ignore requested geometry > Force > ☑ Yes
```

Start Wide

```
>>> Window class (application) > Regular expression > (google-chrome|steam|chrome-[a-z0-9]+-Default|obsidian) 
>>> Match whole window class > ☑ No 
>>> Window types: Normal window 
>>> Window title > Regular expression > (.* - Google Chrome$)|(^Steam$)|(.*) 
>>> Size > Apply initially > 1488 x 1388 
>>> Initial placement > Force > Centred 
>>> Ignore requested geometry > Force > ☑ Yes
```

<br/>
 
### [Next ⇢](4.2-Appearance.md)
